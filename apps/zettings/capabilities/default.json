{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline webview capabilities for the Zettings shell. Least privilege: only core IPC surface; plugin permissions are added individually as they are introduced.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
