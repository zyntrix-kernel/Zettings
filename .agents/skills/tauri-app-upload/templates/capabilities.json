{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for File Upload",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "upload:default",
    {
      "identifier": "upload:allow-upload",
      "allow": [
        {
          "url": "https://api.example.com/upload"
        }
      ]
    }
  ]
}
