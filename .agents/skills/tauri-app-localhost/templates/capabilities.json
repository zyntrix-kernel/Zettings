{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for Localhost",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "localhost:default"
  ]
}
