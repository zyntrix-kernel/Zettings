[package]
name = "rust-observability-golden"
version = "0.0.0"
edition = "2024"
rust-version = "1.85"
publish = false

[dependencies]
tracing = "0.1.44"
tracing-subscriber = { version = "0.3.23", default-features = false, features = ["registry"] }

[lib]
path = "src/lib.rs"
