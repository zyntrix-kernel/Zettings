interface:
  display_name: "Rust Workspace"
  short_description: "Choose project-sized Rust workspace topology and crate boundaries"
  default_prompt: "Use $rust-workspace to derive crate boundaries first, then choose a small root-flat, growing hybrid, or large contained/grouped workspace from project evidence, or diagnose a mixed root-package layout."
