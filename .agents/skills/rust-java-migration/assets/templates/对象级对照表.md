# {{MODULE_NAME}} Java → Rust 对象级对照表（验收清单）

> 生成日期：{{GENERATED_DATE}}
> Java 基线：`{{JAVA_BASELINE}}`
> Rust 基线：`{{RUST_BASELINE}}`
> 最近按 Rust 基线审计：{{AUDITED_DATE}}
> 文档状态：`{{DOCUMENT_STATUS}}`
> Java 模块：`{{JAVA_ROOT}}`
> Java 包根：`{{JAVA_PACKAGE_ROOT}}`
> Rust 目标：`{{RUST_ROOT}}`

<!-- current-migration-contract-start -->
## 当前迁移规范执行口径

- 本文是对象完成状态的唯一当前权威台账；模板生成结果是 `DRAFT`，不得当作完成证据。
- 预期路径从 `{{JAVA_PACKAGE_ROOT}}` 起保留末 `{{RETAIN_SEGMENTS}}` 层包目录并转换为 `snake_case`；必须同时记录预期路径与当前路径。
- Spring/Java 源码决定对象、目录、文件和公共语义边界；Rust 依赖只决定哪些逻辑可精确复用，不能反向取消源对象台账。
- 只有 `IMPLEMENTED`、`DEPENDENCY_REUSED`、`PLATFORM_NA` 属于已处理；`MISSING`、`MISPLACED`、`STUB`、`PARTIAL`、`UNVERIFIED` 都是迁移阻断项。
- 历史详细版的有效设计说明合并到本文末尾历史附录；当前事实区必须先由源码与工作树重新生成，历史状态不得覆盖它。
<!-- current-migration-contract-end -->

本文回答“每个 Java 对象落在哪个 Rust 文件、有哪些方法、当前证据到哪一级”。语义是否一致由《语义迁移对照表》记录。

> 执行规则：B0 在编码前一次性登记完整对象和签名分母；B2 只进行整批实现，
> 不逐对象比对、测试或验收；V0 在全部实现冻结后统一审计并批量更新状态和证据。

## 一、目标工程结构

```text
{{MODULE_NAME}}/
├── Cargo.toml
└── src/
    ├── lib.rs                 # 仅模块声明、文档和 re-export
    ├── <object>.rs            # 一个 Java 对象对应一个主要文件
    └── <penultimate_package>/<last_package>/
        ├── mod.rs             # 仅模块声明和 re-export
        └── <object>.rs
```

<!-- TODO：用真实目标树替换以上示意。 -->

## 二、状态图例

| 状态 | 含义 | 计入结构 | 计入实现 | 计入行为验证 |
|---|---|---:|---:|---:|
| `MISSING` | 预期文件不存在 | 否 | 否 | 否 |
| `MISPLACED` | 同名/对应文件存在，但不在确定性预期路径 | 否 | 否 | 否 |
| `STUB` | 仅形态、空逻辑、占位字段、`todo!()` 或 `unimplemented!()` | 是 | 否 | 否 |
| `PARTIAL` | 只有部分分支、生命周期、副作用或错误语义 | 是 | 否 | 否 |
| `UNVERIFIED` | 实现看似完整，但缺中文来源注释或语义测试证据 | 是 | 否 | 否 |
| `IMPLEMENTED` | 预期路径、真实逻辑、中文来源注释与语义测试均完整 | 是 | 是 | 是 |
| `DEPENDENCY_REUSED` | 精确上游符号已实现，并有版本/提交、适配器和本地集成测试证据 | 不要求复制本地对象文件 | 是 | 是 |
| `PLATFORM_NA` | 经证据证明仅属于 JVM/字节码/类加载器等平台能力 | 单列 | 是 | 不适用 |
| `RUST_EXTENSION` | Rust 特有能力 | 单列 | 不进入 Java 分母 | 不进入 Java 分母 |

## 三、统计汇总

| 分类 | Java 对象 | Rust 主文件 | 已实现 | 行为已验证 | 排除/受阻 |
|---|---:|---:|---:|---:|---:|
| 类 | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> |
| 接口 | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> |
| 枚举 | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> |
| record | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> |
| 注解 | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> |
| 异常 | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> |

## 四、对象映射

每个 Java 对象一行。内部类仅在确实由主对象独占时合并，并写明理由。

| Java 全限定名 | 类型 | Java 文件/符号 | 预期 Rust 路径 | 当前 Rust 路径/符号 | Rust 类型 | Java/Rust 公共方法数 | 来源注释 | 状态 | 实现与测试证据 | 说明 |
|---|---|---|---|---|---|---:|---|---|---|---|
| <!-- TODO --> | class/interface/enum/record/annotation/exception | <!-- TODO --> | <!-- 末两层 + snake_case --> | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | 完整/部分/缺失 | `MISSING` | <!-- 当前 SHA 的命令/测试/artifact --> | <!-- TODO --> |

## 五、方法与重载明细

每个 Java 签名单独一行，不得只登记方法名。

| Java 对象 | Java 完整签名 | 参数名/顺序/默认/可空 | Rust 完整签名 | 映射方式 | 重载命名策略 | JavaDoc 映射 | 返回/错误映射 | 状态 | 证据 |
|---|---|---|---|---|---|---|---|---|---|
| <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | `DIRECT` / `ADAPTED` | canonical / `_with_*` / trait / options | 对象/方法/类型参数/参数/返回/异常/元数据/行内注释 | <!-- TODO --> | `MISSING` | <!-- TODO --> |

## 六、合并、拆分与重命名

> `ADAPTED` 是 API/兼容层映射方式，不是完成状态；必须同时填写事实状态和验证证据。

| Java 对象/签名 | Rust 处理 | 类型 | 原因 | 兼容影响 | 批准/证据 |
|---|---|---|---|---|---|
| <!-- TODO --> | <!-- TODO --> | 合并/拆分/重命名/替代 | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> |

### 文件职责与大小审查

| Java 对象 | 主 Rust 文件 | 物理行数 | 分级 | 职责审查/拆分决定 | 子实现文件 | 是否引入第二对象 | 证据 |
|---|---|---:|---|---|---|---|---|
| <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | NORMAL ≤500 / REVIEW 501–800 / BLOCKED >800 | validation/conversion/lifecycle/... | <!-- TODO --> | 否 | <!-- TODO --> |

## 七、Java 特有与 Rust 扩展

### Java 特有

| Java 对象 | 原因 | Rust 替代 | 影响 | 状态 |
|---|---|---|---|---|
| <!-- TODO --> | JVM/字节码/类加载器证据 | <!-- TODO --> | <!-- TODO --> | `PLATFORM_NA` |

### 精确依赖复用

| Java 对象/职责 | crate | 版本/提交 | 上游源码符号 | 本地适配器/调用点 | 集成测试与命令 | 状态 |
|---|---|---|---|---|---|---|
| <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | `DEPENDENCY_REUSED` |

### Rust 扩展

| Rust 文件/类型 | 目的 | 是否影响兼容门面 | 测试 | 状态 |
|---|---|---|---|---|
| <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | `RUST_EXTENSION` |

## 八、结构验收

- [ ] Java 对象数可从固定 SHA 重算。
- [ ] Rust 主文件排除 `lib.rs`、`mod.rs`、生成代码后可重算。
- [ ] 每个对象只有一个主要 Rust 文件。
- [ ] 所有生产、测试、示例、bench 与测试支持 `.rs` 文件均不超过 800 个物理行；501–800 行文件已有职责审查。
- [ ] 超长或弱内聚对象按职责拆分，子文件只实现同一 Java 对象，不引入第二迁移对象。
- [ ] 超过 Clippy 100 行信号的函数/方法已审查，未按行数机械拆分内聚算法。
- [ ] 聚焦私有行为的单元测试可位于 `#[cfg(test)]`；集成、差分、宿主、负载和整体验收测试位于 `tests/` 或测试包。
- [ ] 每个 Java 对象均按 `retain_segments={{RETAIN_SEGMENTS}}` 计算预期路径，错位文件标记为 `MISPLACED`。
- [ ] 每个重载和参数都有映射。
- [ ] 每个已有 Java 对象和方法注释都有 Rust 中文文档对应。
- [ ] 每个 `@param`（含泛型参数）、`@return`、`@throws`、`@since`、`@deprecated` 和相关 `@see` 都有明确去向。
- [ ] 关键 Java 行内/块注释已迁移到对应 Rust 分支或不变量附近。
- [ ] 对象和签名完整分母在编码前冻结。
- [ ] 状态与证据在整批实现冻结后的统一审计中批量更新，而非逐对象升级。
- [ ] 合并、拆分、重命名、排除和扩展均单列。
- [ ] 覆盖率分母未混入 BOM、聚合器、facade、测试支持或 Rust 扩展。
- [ ] 所有 `IMPLEMENTED` 行均有当前 Rust SHA 的文件/符号与可重放语义测试证据。
- [ ] 所有 `DEPENDENCY_REUSED` 行均有精确 crate、版本/提交、上游符号、本地调用点与集成测试。
- [ ] 所有 `PLATFORM_NA` 行均有平台专属证据，未用“生态已有”或“语义类似”代替。
- [ ] 只要存在 `MISSING`、`MISPLACED`、`STUB`、`PARTIAL`、`UNVERIFIED`，模块结论就保持未完成。
- [ ] 本表状态已与路线图、语义表和名称检查交叉核对。

<!-- historical-design-appendix-start -->
## 历史设计附录

<!-- 将旧“历史详细版”中仍有效的设计理由和风险说明合并到这里。旧数量、旧状态和旧完成率仅作历史记录，不得覆盖以上当前事实。 -->
