# Tables Accessibility Skill

Accessible HTML data table patterns: `<caption>`, `<th scope>`, spanned headers,
`headers`/`id`, responsive layouts, sortable columns, and CMS considerations.

**Do not use tables for layout** — see `progressive-enhancement/SKILL.md`.

## Install

Copy this directory into your `skills/` folder, or add to project instructions:

> Before working on data tables: read `skills/tables/SKILL.md`

## Canonical source

`examples/TABLES_ACCESSIBILITY_BEST_PRACTICES.md` in `mgifford/ACCESSIBILITY.md`
(to be created — skill was authored first).
