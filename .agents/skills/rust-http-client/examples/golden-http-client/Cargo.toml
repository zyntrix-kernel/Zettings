[package]
name = "rust-http-client-golden"
version = "0.0.0"
edition = "2024"
rust-version = "1.85"
publish = false

[dependencies]
reqwest = { version = "0.13.4", default-features = false, features = ["blocking", "rustls"] }

[lib]
path = "src/lib.rs"
