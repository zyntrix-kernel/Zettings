{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for WASM (Frontend only, no specific backend plugin)",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
