[package]
name = "zettings-ipc"
description = "Typed IPC contracts between the ZETTINGS webview and the Rust backend, with TypeScript binding generation."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true

[dependencies]
serde = { workspace = true }
zettings-core = { workspace = true }
zettings-search = { workspace = true }
ts-rs = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

[features]
# Enables the test that regenerates committed TypeScript bindings.
export-bindings = []

[lints]
workspace = true
